1. register a0-a7 contain arguments to functions. register a2 holds 13, the third argument, when call printf.
2. in call.asm, the answer to f(8)+1 is calculated by compiler, so it doesn't call function f in main().
3. 638
4. 38
5. H11e0,World
   i would be "0x726c6400" in big-endian case
   no
6. the value of register a1, which we don't know it exactly.